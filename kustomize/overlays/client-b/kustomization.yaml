apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: client-b

resources:
  - ../../base

nameSuffix: -client-b

patches:
  - target:
      kind: Deployment
      name: nginx
    path: env-patch.yaml
